{
  "name": "sparky-ai",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "test": "node --test",
    "check": "node --check public/app.js && node --check api/generate.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.57.4",
    "stripe": "^18.5.0"
  },
  "devDependencies": {
    "vercel": "^48.1.0"
  },
  "engines": { "node": ">=20" }
}
